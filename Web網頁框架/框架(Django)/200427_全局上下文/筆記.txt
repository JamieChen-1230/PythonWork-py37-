創建自定義全局上下文：
   一、在APP下創建一個my_context_processors.py，名稱可自定義
   二、編寫函數
   三、將函數配置到settings.py的context_processors中


全局上下文的應用：
    - 通用的操作
    - 通常是用於在多個模版都要使用的數據，EX: 導航列
    - 優點是這樣可以減少重複代碼，且要維護更新時，只要改動這邊即可，不用每個模板都一一去改
